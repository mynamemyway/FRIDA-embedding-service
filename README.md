# e5-large-embedding-service 🚀

<p align="left">
  <img src="https://img.shields.io/badge/Status-In_Development-blue" alt="Status">
  <img src="https://img.shields.io/badge/Version-0.1.0-blueviolet" alt="Version">
  <img src="https://img.shields.io/badge/Python-3.12+-blue.svg" alt="Python version">
  <img src="https://img.shields.io/badge/Docker-Ready-0db7ed.svg" alt="Docker Ready">
</p>

Высокопроизводительный API-сервис для генерации текстовых эмбеддингов с использованием модели `intfloat/multilingual-e5-large`. Сервис контейнеризирован с помощью Docker и предназначен для развертывания на собственном сервере.

## 📚 Оглавление
- Стек технологий
- Структура проекта
- Справка по API
- Инструкции по запуску

## 🔧 Стек технологий

<p align="left">
  <img src="https://img.shields.io/badge/FastAPI-Web_Framework-009688.svg" alt="FastAPI">
  <img src="https://img.shields.io/badge/Sentence--Transformers-Embeddings-D81B60.svg" alt="Sentence-Transformers">
  <img src="https://img.shields.io/badge/Pydantic-Data_Validation-e9b404.svg" alt="Pydantic">
  <img src="https://img.shields.io/badge/Uvicorn-ASGI_Server-green.svg" alt="Uvicorn">
</p>

## 📁 Структура проекта

```text
e5-large-embedding-service/
├── app/
│   ├── main.py         # Код FastAPI приложения
│   └── models.py       # Pydantic модели для запросов/ответов
├── .gitignore
├── Dockerfile          # Инструкции для сборки Docker-образа
├── README.md
└── requirements.txt    # Зависимости Python
```

## 📖 Справка по API

Сервис предоставляет один эндпоинт для векторизации текста.

### `POST /embed`

Принимает список текстов и возвращает их векторные представления (эмбеддинги).

**Тело запроса (Request Body):**

```json
{
  "texts": ["Пример текста для векторизации", "Еще один текст"]
}
```

**Пример ответа (Response Body):**

```json
{
  "embeddings": [
    [0.0123, -0.0456, ..., 0.0789],
    [-0.0987, 0.0654, ..., -0.0321]
  ]
}
```

## 📦 Инструкции по запуску

Сервис предназначен для запуска в виде Docker-контейнера.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/mynamemyway/e5-large-embedding-service.git
    cd e5-large-embedding-service
    ```

2.  **Соберите Docker-образ:**
    Эта команда создаст образ с именем `e5-embedding-service`.
    ```bash
    docker build -t e5-embedding-service .
    ```

3.  **Запустите Docker-контейнер:**
    Команда запустит контейнер в фоновом режиме и пробросит порт `8000` хоста на порт `8000` контейнера.
    ```bash
    # При первом запуске начнется скачивание модели (~2.24 ГБ), что может занять несколько минут.
    docker run -d -p 8000:8000 --name e5-service e5-embedding-service
    ```

4.  **Проверьте логи и статус загрузки:**
    После запуска контейнера, особенно в первый раз, необходимо убедиться, что модель успешно загрузилась.
    ```bash
    docker logs -f e5-service
    ```
    Следите за логами. Вы должны увидеть сообщение `INFO: Модель успешно загружена.`. Для выхода из режима просмотра логов нажмите `Ctrl+C`.

5.  **Протестируйте эндпоинты:**

    *   **Проверка состояния (`/health`):**
        ```bash
        curl http://localhost:8000/health
        ```
        Ожидаемый ответ: `{"status":"ok"}`

    *   **Проверка основного функционала (`/embed`):**
    ```bash
    curl -X POST -H "Content-Type: application/json" -d '{"texts": ["проверка работы сервиса"]}' http://localhost:8000/embed
    ```
    В ответ вы получите JSON с векторным представлением текста.

6.  **Остановка и удаление контейнера:**
    Для остановки и последующего удаления контейнера используйте следующие команды:
    ```bash
    docker stop e5-service
    docker rm e5-service
    ```
